{{> header }}
<div class="container-fluid">
    <h1 class="text-center">{{noticia.titulo}}</h1>
    <div class="row">
        <div class="col-sm-1"></div>
        <div class="col-sm-10">
        <div class="row">
            <div class="col-sm-7">
                <p>{{noticia.contenido}}</p>
            </div>
            <div class="col-sm-5">
                <img src="{{noticia.image_path}}" class="rounded float-right" alt="{{noticia.image_path}}" style="max-width:450px">
            </div>
            
        </div>
        

        </div>
        <div class="col-sm-1">
        </div>
    </div>
</div>
</div>

{{> footer }}


